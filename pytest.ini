[pytest]
# Markers only. Deliberately no testpaths/addopts: this repo's suite is run
# ad hoc against several interpreters (the box's 3.10, a local venv), and
# pinning discovery here would change how existing runs behave.
markers =
    unit: a fast, isolated test with no I/O
    integration: exercises a real boundary (box filesystem, GPU, network)
